# Config chargée automatiquement uniquement en développement (NODE_ENV=development,
# ex: yarn start:dev). N'affecte pas la production (config.production.yml).
# Voir config/config.server.ts pour l'ordre de priorité.

# Backend REST utilisé en local : la démo publique DSpace, pour ne pas dépendre
# du backend de prod (umontreal-dev.scholaris.ca).
rest:
  ssl: true
  host: demo.dspace.org
  port: 443
  nameSpace: /server
